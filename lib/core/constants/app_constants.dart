class AppConstants {
  // Animation durations
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration scooterAnimationDuration = Duration(milliseconds: 1200);
  static const Duration textAnimationDuration = Duration(milliseconds: 800);
  static const Duration waveAnimationDuration = Duration(milliseconds: 3000);
  
  // Animation delays
  static const Duration textAnimationDelay = Duration(milliseconds: 400);
  
  AppConstants._();
}
